name: "Webhook Event example"

on:
  repository_dispatch:
    types:
      - webhook

jobs:
  respond-to-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo   # Added the missing colon here
        uses: actions/checkout@v4 # Updated to v4 for better security/performance

      - name: Run a script
        run: echo "Event of type: $GITHUB_EVENT_NAME"
